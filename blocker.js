/// blocker.js
(function() {
    'use strict';
    console.log("%c █ BLOCKER.JS LOADED SUCCESSFULLY █", "background:#f00;color:#fff;font-size:20px");
    console.log("Timestamp:", new Date().toISOString());
    console.log("URL:", location.href);

    // Your original code will go back here later — for now we just want proof it runs
    alert("blocker.js is running via uBlock!");   // optional, very loud proof

    // ←←← Paste your full original script below this line once you confirm it loads
})();